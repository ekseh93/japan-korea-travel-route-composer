{
  "name": "@route-composer/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "3.1110.0",
    "@aws-sdk/lib-dynamodb": "3.1110.0",
    "@route-composer/contracts": "workspace:*",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "26.2.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  }
}
