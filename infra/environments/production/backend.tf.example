terraform {
  backend "s3" {
    bucket       = "REPLACE_WITH_APPROVED_STATE_BUCKET"
    key          = "production/terraform.tfstate"
    region       = "ap-northeast-1"
    encrypt      = true
    use_lockfile = true
  }
}
